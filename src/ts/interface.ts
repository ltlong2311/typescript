// interface

interface Contact {
    name: string;
    phone: number;
    email: string;
}

const contacts: Contact[] = [];

const newContact: Contact = {
    name: "LTL",
    phone: "number",
    email: string;
}